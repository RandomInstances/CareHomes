import type { NextConfig } from "next";

// The directory is database-backed, so every home has its own indexable URL.
// The original prototype stays at /prototype.html for reference.
const nextConfig: NextConfig = {};

export default nextConfig;
